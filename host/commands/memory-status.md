---
description: Show both memory stores, what this project can see, and whether semantic ranking is available.
---

Call the `memory_status` tool from the vestige MCP server and present the result. Then call `recall` with a limit of 10 and summarise what this project can currently see, grouped by store. If nothing is visible, say so plainly and mention that `remember` is how the first memory gets there.
